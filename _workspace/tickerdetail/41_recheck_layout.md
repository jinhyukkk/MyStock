# 41. TickerDetail 회귀 탐지 계측 (악화·결함만)

- 계측일: 2026-08-21 / layout-auditor
- 대상: `/ticker/AAPL`, `/ticker/000660`, `/ticker/AAPL/analysis`, `/ticker/000660/analysis`
- 서버: **8722 = 빌드본(사용자가 보는 것)**, **5173 = dev(소스)**. 전용 탭 `tab-2`(8722)·`tab-3`(5173)만 사용, 기존 탭 미접촉.
- 스크린샷 도구 사용 불가(`Browser pane is not displayed` — 5s 타임아웃). **전 항목 `read_page` + `javascript_tool` 실측**으로 대체. 스크린샷 근거는 전부 "미확인, DOM 계측치로 대체".
- `/`(대시보드) 미계측(브리프 §검수 제외).

---

## 1. 경로 × 폭 PASS/FAIL 표 (8722 빌드본)

| 계측 항목 | AAPL 1280 | AAPL 390 | 000660 1280 | 000660 390 | AAPL/analysis 1280 | AAPL/analysis 390 | 000660/analysis 1280 | 000660/analysis 390 |
|---|---|---|---|---|---|---|---|---|
| 1. 페이지 가로 넘침 `scrollWidth<=innerWidth` | PASS 1265/1280 | PASS 390/390 | PASS 1265/1280 | PASS 390/390 | PASS 1265 | PASS 390 | PASS 1265 | PASS 390 |
| 1b. **내부 컨테이너 넘침** | PASS | PASS | PASS | PASS | **FAIL** 812/798 | **FAIL** 788·812 / 342 | **FAIL** 812/798 | **FAIL** 792·812 / 342 |
| 2a. `.snap-cell` 84칸 | PASS 84 | PASS 84 | PASS 84 | PASS 84 | n/a | n/a | n/a | n/a |
| 2b. 칸 값 넘침 | PASS 0 | PASS 0 | PASS 0 | PASS 0 | n/a | n/a | n/a | n/a |
| 2c. **라벨 말줄임** | PASS 0 | **FAIL 4칸** | PASS 0 | **FAIL 4칸** | n/a | n/a | n/a | n/a |
| 3. `undefined/NaN/null/Infinity/[object` | PASS 0 | PASS 0 | PASS 0 | PASS 0 | PASS 0 | PASS 0 | PASS 0 | PASS 0 |
| 4. 빈 상태 이유 문구 | PASS | PASS | **부분 FAIL**(내부자 헤더) | 동일 | n/a | n/a | n/a | n/a |
| 5. 재무 차트 3 svg / 토글 | PASS 3svg, 4→5막대 | PASS | **부분** 2svg + note(설계대로) | 동일 | n/a | n/a | n/a | n/a |
| 5b. 추정 막대 구분 | **검증 불가**(추정 0건) | 동일 | PASS opacity 0.35 + `(E)` | PASS | n/a | n/a | n/a | n/a |
| 6. 내부자 표 카드 전환 | n/a | PASS `thead:none`/`td::before` 라벨 | n/a(unavailable) | n/a | n/a | n/a | n/a | n/a |
| 7. 숫자 표기 | **FAIL** (아래 D1·D2) | FAIL | **FAIL** (D1~D5) | FAIL | **FAIL** (D8·D9) | FAIL | **FAIL** (D10) | FAIL |
| 8. 중복 정보 3회+ | PASS(최대 2회) | PASS | PASS | PASS | PASS | PASS | PASS | PASS |
| 9. 8722 vs 5173 일치 | **FAIL — 낡은 빌드** | FAIL | **FAIL** | FAIL | 미계측 | 미계측 | 미계측 | 미계측 |

---

## 2. FAIL 상세

### D1. 두 값 한 칸이 붙어 하나의 숫자로 읽힌다 — 최악 결함
- 셀렉터: `.snap-value > small`, `margin-left: 4px`, `font-size: 11px`(값은 13px)
- 실측(8722·5173 동일):
  - `172,0439.94%` (`ATR (14)` = 172,043 + 9.94%) — 000660
  - `3,0000.09%` (`최근 배당(주당)` = 3,000 + 0.09%) — 000660
  - `2,987,000-42.08%` (`52주 고가`) — 000660
  - `1.050.34%`, `344.27-9.58%`, `7.792.5%`, `326.34+4.83%`, `1.36%2.13%` — AAPL
  - `+165.07%—`, `+26.50%+15.73%` — 000660
- 근거: `SnapshotTable.tsx:26-27` — `{c.value}{c.sub && <small>{c.sub}</small>}` 사이에 구분자 없음. 4px 여백만으로 `172,043`과 `9.94%`가 분리되지 않는다.
- 수정: `<small>` 앞에 얇은 구분자(`·` 또는 `margin-left: 10px` + 좌측 1px divider)를 넣거나, `sub`를 두 번째 줄로 내린다.

### D2. 스냅샷 출처 줄만 ISO 원시 타임스탬프
- 실측: `출처: yfinance · naver · daum · 2026-08-21T15:52:54` (000660), `출처: yfinance · 2026-08-21T16:00:33` (AAPL, dev)
- 나머지 5개 블록은 전부 `출처: naver · 3분 전` (상대시각)
- `frontend/src/pages/TickerDetail.tsx:216` — `` {detail.snapshot?.fetched_at && ` · ${detail.snapshot.fetched_at}`} ``. `BlockEmpty.tsx:36`의 `relativeTime(...)`을 쓰지 않는 유일한 지점.
- 8722·5173 **양쪽 모두 미수정** → 다음 빌드에도 나간다.

### D3. KRW 종목에 USD 축약 단위(`M`)가 섞인다
- 실측 000660: `평균 거래량(20) = 5.73M` ↔ 같은 열의 `발행주식수 = 7.1억`, `유통주식수 = 5.7억`
- **`frontend/src/quote/snapshotCells.ts:150` — `abbrNum('USD', avgVolume(c, 20))` 통화 하드코딩.** 스펙 §4.3 표기 규칙(KRW `12.3조`/`4,560억`) 위반, AC-21 위반.
- 8722·5173 모두 미수정.

### D4. 조 단위 축약이 4자리에서 무너진다
- 실측 000660: `시가총액 1263.8조`, `기업가치(EV) 1131.2조` — 천단위 구분 없음
- `frontend/src/quote/fmt.ts:22` — `if (a >= 1e12) return (a/1e12).toFixed(1) + '조'`. 1e15 이상 분기가 없어 `1263.8조`가 나온다(브리프가 비정상 예로 든 `1235.15조`와 동일 형태).
- 수정: 조 단위도 `toLocaleString('ko-KR')`로 `1,263.8조`.

### D5. 당좌비율 1.33x vs 유동비율 17.54x — ÷100 정규화 누락 의심
- 실측 000660(8722·5173 동일): `당좌비율 1.33` / `유동비율 17.54` — 정의상 유동비율 ≥ 당좌비율이지만 **13배** 차이는 한쪽만 ÷100된 신호.
- 스펙 §5.1 "배수" 규칙 + §10 리스크 "비율 단위 혼동"에 정확히 해당. `snapshotCells.ts:79-80`은 FE 책임이 아니고 BE 정규화 문제.
- 판정: **미확인(BE 확인 필요)**, 단 화면에는 이미 모순된 두 값이 나란히 서 있다.

### D6. 뉴스 제목에 HTML 엔티티 원문 노출 + 소스단 말줄임
- 실측 000660(8722·5173 동일): `&quot;` 리터럴 **2건** — `[글로벌 증시 따라잡기] 베선트 바이백 1일 천하? &quot;미 장기 국채 불안...`
- 20건 중 **7건**이 `...`로 끝나는 소스 절단 제목. 1280px에서 가로 여유가 충분한데도 잘려 온다(CSS 말줄임 아님 — 텍스트에 `...`가 들어 있음).
- `NewsList.tsx:23`은 `{n.title}`을 그대로 렌더하므로 근본 원인은 BE(Naver 응답 언이스케이프·절단 미복원).

### D7. `unavailable` 블록이 근거 없는 갱신시각을 단다
- 실측 000660 내부자 블록 헤더: `내부자 거래` / `3분 전` — `출처:` 접두가 없어 시각만 떠 있다. 아무것도 받아오지 않은 블록인데 "3분 전"이 붙는다.
- `frontend/src/components/quote/BlockEmpty.tsx:34-38` — `source`가 null이면 `parts`에 시각만 들어간다.
- 회사 설명 블록: `출처: yfinance+daum+fdr` — 다른 블록의 ` · ` 구분자와 다르고 갱신시각이 없다(`TickerDetail.tsx:246`). 스펙 B1("출처: yfinance · 네이버 · 3시간 전") 미충족.

### D8. 헤더 현재가가 센트를 잃는다
- 실측 AAPL(개요·분석 양쪽, 8722·5173): 헤더 `$311.3` ↔ 스냅샷 `현재가 311.30`, `전일 종가 316.83`
- `QuoteHeader.tsx:44` → `format.ts:9` `cur()`가 `fmt()`의 가변 소수점을 쓴다. 같은 화면에서 같은 값이 서로 다른 자릿수로 두 번 나온다.
- format.ts는 전역 공용(스펙 §8.3 회피 대상) → 헤더 쪽에서 `moneyCell` 계열로 고쳐야 한다.

### D9. `/analysis` 진입 플랜이 $와 ₩을 환율 없이 섞는다
- 실측 AAPL/analysis: `제안 손절가 $295.71` · `제안 목표가 $342.47` · **`리스크 ₩1,762,407`** · **`평가액 ₩34,908,565`** — 같은 블록, 환율·기준시각 표기 없음.
- 같은 줄에 `(계산값 81.679)` 원시 부동소수 노출.

### D10. `/analysis` 청산 플랜이 소수점 주식수를 제시한다
- 실측 000660/analysis: `1/3 → 수량 1.667`, `1/2 → 수량 2.5`
- 국내 주식은 소수점 매도가 불가. AAPL 쪽에는 "1주 단위 내림" 문구가 있는데 청산 플랜에는 없다 — 같은 화면 안에서 규칙이 갈린다.
- 부수: `평가손익 -20.90%₩-2,285,640`, `R 배수 -1.33R1R = ₩344,087` — D1과 같은 붙임 결함이 분석 탭에도 있다.

### D11. 390px 스냅샷 라벨 4칸 말줄임
- 실측 8722 · AAPL/000660 공통(`.snap-label` scrollWidth/clientWidth):
  - `EPS 성장(과거 3/5년)` 102/82
  - `매출 성장(과거 3/5년)` 103/88
  - `배당성장 3/5년` 72/57
  - `서프라이즈(EPS/매출)` 104/87
- 1280px에서는 0칸. 모바일에서 어떤 지표인지 식별 불가.

### D12. 390px에서 `분석` 탭이 화면 밖으로 잘린다
- 실측(8722·5173 동일): `NAV.quote-tabs scrollWidth 408 / clientWidth 342`, 마지막 `분석` 탭 right = **432px** (뷰포트 390px)
- `overflow-x: auto`라 스크롤은 되지만 그라데이션·화살표 등 어포던스 없음.
- 스펙 §7이 못박은 대로 **손절·목표 룰 등록 UI는 앱 전체에서 `/analysis`에만 있다.** 모바일에서 그 유일한 진입점이 안 보인다.

### D13. `/analysis` 백테스트 표가 1280px에서 넘친다 — **기준선 위반**
- 실측: `DIV.table-scroll scrollWidth 812 / clientWidth 798` (AAPL·000660 양쪽, 1280px)
- 390px에서는 788·812 / 342 = **2.3배 가로 드래그**. `.table-cards`가 아니라 `.table-scroll`이라 카드로 접히지 않는다.
- 11열(등급·신호 일수·독립 표본·60일 평균·120일 평균·60일 승률·120일 승률·60일 손절률·120일 손절률·60일 초과·120일 초과) — 열 수 자체가 과잉.

---

## 3. 삭제·축소 후보 (근거 수치 포함)

| 대상 | 근거 실측 | 처리 |
|---|---|---|
| 내부자 표 `보유 총수` 열 | AAPL 30행 중 **30행 `—`**(100%) | 열 삭제 |
| 내부자 표 `공시` 열 | AAPL 30행 중 **30행 `—`**(100%). SEC Form 4 링크가 한 건도 없다 | 열 삭제 (링크가 채워지면 이름 셀에 링크로 부착) |
| 내부자 표 `단가`·`금액` | 30행 중 16행 `—`, 4행이 `0.00`/`0`(Stock Gift). 실값 10/30 | `0`은 `—`로. 열은 유지 |
| 내부자 표 행 수 | 390px 카드 1장 **243px × 30행 = 7,507px**(844px 화면의 8.9배). 전체 문서 15,023px = 17.8스크린 | 기본 10행 + "더 보기" |
| `EPS 성장(과거 3/5년)`·`매출 성장(과거 3/5년)`·`배당성장 3/5년`(AAPL 3/5년 중 5년이 `—`) | 3칸 모두 `+6.88%—` 형태 | `snapshotCells.ts:103` 주석이 이미 같은 논리로 `서프라이즈`를 정리했다 — 나머지 3칸도 단일 값으로 |
| 재무 차트 범례 "반투명 막대 + (E) = 컨센서스 추정치" | AAPL 연간·분기 **추정 막대 0개**(전부 `fill-opacity 0.9`) | 추정 막대가 있을 때만 표시 |
| 차트 하단 "Charting by TradingView" | 한 화면에 **3회** 반복(RSI·MACD·메인) | 1회로 |
| 백테스트 안내문 | `/analysis` 표 위 약 230자 5문장 | 2문장 + 접기 |

---

## 4. 부족한 것

1. **성과 열의 빠진 칸(빌드본 한정)** — 8722에서 `1주/1개월/3개월/6개월/연초/1년/3년/**5년 —**/10년`. 5173에서는 AAPL `+110.21%`, 000660 `+1613.17%`로 채워져 있다 → **낡은 빌드 때문**. 배포하면 해소.
2. **컨센서스 의견의 언어 라벨** — 스냅샷 칸은 `2.11`만 표시(스케일은 `title` 툴팁에만). 애널리스트 블록은 `2.11매수`. 스냅샷 칸 `sub`에 `매수`를 넣어 통일.
3. **애널리스트 수 옆의 정체불명 날짜** — 000660 `40명2026-08-20`. 라벨(`기준일`) 없이 붙어 있다. 라벨 부여 또는 블록 상단 갱신시각으로 승격.
4. **뉴스 관련성 표시** — 000660 20건 중 상당수가 코스피/코스닥 시황·삼성전자·카카오·앤트로픽 기사. 종목명 매칭 배지나 "시황" 태그가 없어 종목 뉴스와 구분되지 않는다.
5. **모바일 탭 스크롤 어포던스** — D12. 우측 페이드 또는 `분석`을 헤더 버튼으로 승격.

---

## 5. 유지 (건드리지 말 것)

1. **`unavailable`/`shares_note` 이유 문구** — `국내 종목 내부자 거래는 OpenDART 키(무료)를 등록해야 표시됩니다.` / `발행주식수 이력은 OpenDART 키 등록 후 표시됩니다.` / `국내 종목은 증권사별 투자의견 변경 이력을…리포트 목록으로 대신합니다.` 빈 표 껍데기·빈 차트가 **0건**이고, 대신 왜 없는지가 나온다. AC-8 PASS.
2. **백테스트 `검증 불가` 억제** — 000660 중장기 독립 표본 13/7에서 평균·초과수익을 숨기고 경고를 띄운다. 표본 부족을 숫자로 덮지 않는 유일한 장치.
3. **000660/analysis 손절폭 경고** — `⚠ 2×ATR 손절폭이 19.89%로 스윙 타임프레임에 과대합니다 (기준 15%)`. 실수 방지 장치.
4. **내부자 표 모바일 카드 라벨** — `theme.css:233` `td::before { content: attr(data-label) }`가 실제로 붙는다(실측: 관계/날짜/거래/단가/수량/금액/보유 총수/공시). AC-18 구조 PASS.

---

## 6. 운영 이슈

### O1. 빌드본(8722)이 소스(5173)보다 낡았다 — 사용자가 구버전을 본다
`.snap-label` 텍스트 diff (AAPL 84칸):

| # | 8722 (빌드본) | 5173 (dev/소스) |
|---|---|---|
| 50 | `EPS 전년동기` | `EPS 전년동기(분기)` |
| 55 | `당좌비율(배)` | `당좌비율` (값 `1.33x`) |
| 56 | `매출 전년동기` | `매출 전년동기(분기)` |
| 61 | `유동비율(배)` | `유동비율` (값 `17.54x`) |
| 67 | `부채비율(배)` | `부채비율` |
| 73 | `장기부채비율(배)` | `장기부채비율` |
| 80 | `서프라이즈(EPS/매출)` = `+6.74%—` | `EPS 서프라이즈` = `+6.74%` |

값 diff: `5년` 8722 `—` → 5173 AAPL `+110.21%` / 000660 `+1613.17%`.
`최근 배당(주당)` 부가값 8722 `0.09%` → 5173 `0.17%` (= 3,000/1,730,000, **빌드본이 틀린 값**).
→ `npm run build` 미실행. AC 검수를 8722에서만 하면 이미 고쳐진 것을 결함으로, 아직 안 고쳐진 것을 통과로 잘못 판정한다.

### O2. 5173 콘솔 502 Bad Gateway 2건
dev 프록시가 백엔드로 붙지 못한 요청. 재현 조건 **미확인**(내비게이션 중 1회). 8722 콘솔 에러 0건.

### O3. 스크린샷 도구 불능
`Browser pane is not displayed` — 이 라운드의 모든 근거는 DOM 계측치다. 시각적 대비·색상·행간은 **미검증**.

---

## 7. 기준선 대비 악화 항목 (직전 커밋 eb6f6d3 기준선: 1280 가로 넘침 0 · 스냅샷 칸 넘침 0 / 390 가로 스크롤 0 · 2쌍/행)

| # | 악화 | 기준선 | 현재 실측 | 위치 |
|---|---|---|---|---|
| **R1** | **1280px 내부 컨테이너 가로 넘침 0 → 2** | 넘침 0 | `/ticker/AAPL/analysis`·`/ticker/000660/analysis` `DIV.table-scroll` **812 / 798** | §7 백테스트 JSX 이동분 |
| **R2** | **390px 스냅샷 라벨 말줄임 0 → 4칸(빌드본) / 5칸(소스)** | 칸 넘침 0 | build: `EPS 성장(과거 3/5년)` 102/82, `매출 성장(과거 3/5년)` 103/88, `배당성장 3/5년` 72/57, `서프라이즈(EPS/매출)` 104/87. **dev에서 `EPS 전년동기(분기)` 92/89 · `매출 전년동기(분기)` 94/93이 새로 추가되어 5칸으로 늘었다** | `quote/snapshotCells.ts:98-99`(라벨 개명이 원인), 84칸 격자 |
| **R3** | **390px 내부 가로 스크롤 0 → 2컨테이너 2.3배** | 가로 스크롤 0 | `/analysis` `DIV.table-scroll` 788·812 / 342 | 백테스트 표(11열) |
| **R4** | 빌드본이 소스보다 낡음 — 라벨 7칸·값 2칸 불일치 | 해당 없음 | O1 표 | 배포 파이프라인 |

> R1·R3은 페이지 레벨(`documentElement.scrollWidth`)만 재면 PASS로 보인다. 기준선 계측이 페이지 레벨만 봤다면 이 두 항목은 **처음부터 놓친 것**일 수 있다 — 그 경우 "악화"가 아니라 "기준선 계측 누락". 어느 쪽인지는 **미확인**.

---

## 8. 우선순위 1개

**D1 — `.snap-value > small` 붙임 결함을 고친다** (`SnapshotTable.tsx:26-27`, `.snap-value small { margin-left: 4px }`).

이유: 84칸 중 **10칸 이상**이 영향을 받고, 그중 `172,0439.94%`·`3,0000.09%`·`2,987,000-42.08%`는 두 숫자가 하나의 숫자로 읽힌다. 다른 결함은 값이 없거나 낡은 것이지만 이것은 **있는 값을 틀리게 읽히게 만든다.** 스냅샷 84칸이 이번 개편의 핵심 산출물인데, 그 안에서 가장 정보 밀도가 높은 칸(52주 고저·ATR·목표주가·배당)이 전부 여기에 걸린다. 구분자 하나로 10칸이 동시에 해결된다.
